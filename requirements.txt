# Room Safety Monitor - Python dependencies
# Target interpreter: Python 3.11 or 3.12. Do not use 3.14; several ML wheels
# (ultralytics, torch, mmcv, mmpose, pytorchvideo) are not yet published for it.

# --- Service plane (L6): API + WebSocket ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Persistence (L5): Postgres + object store client ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
minio>=7.2

# --- L2 perception ---
ultralytics>=8.1          # detection (YOLOv8)
supervision>=0.21,<0.30   # ByteTrack (deprecated in 0.28, removed in 0.30 — pin below removal)
opencv-python>=4.9
rtmlib>=0.0.15            # pose (RTMPose via ONNX). Used instead of mmpose/mmcv:
onnxruntime>=1.17         #   mmcv has no wheel for torch 2.12+cu130 and needs nvcc.
pytorchvideo>=0.1.5       # action (SlowFast). NOTE: pytorchvideo.transforms is broken
                          #   on torchvision 0.27; we hand-roll preprocessing instead.

# --- L3 VLM (Qwen 2.5 VL via HF Inference Providers) ---
requests>=2.31
python-dotenv>=1.0

# --- Tests ---
pytest>=8.0
httpx>=0.27
