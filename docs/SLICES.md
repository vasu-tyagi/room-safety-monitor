# Build plan: six-layer rebuild

Source of truth for the rebuild structure until `docs/ARCHITECTURE.md` is
written in Slice 9. The old four-tier design stays at `docs/architecture.md`
as a historical record of v0.5.

## Target architecture (six layers)

| Layer | Name | What it does |
|-------|------|--------------|
| L1 | Stream Ingest | Multicast RTSP receiver (per-camera workers, auto-reconnect), NVDEC H.264 decode (30 fps, up to 640x360), batcher + ByteTrack persistent identity, ROI crop per camera/room. |
| L2 | Fast Classical CV | Triton + TensorRT, <=50 ms/frame, every frame. Detection (YOLOv8: person/object/vehicle + IDs), Pose (RTMPose: 17 COCO keypoints, fall/contact cues from geometry), Action (SlowFast Kinetics-400: running/falling/fighting). |
| — | Event Gate | ~1% funnel, 99% filtered. Rules over L2: fall_pose_detected, action_in_target_set ({falling, fighting, running}), two_persons_close_proximity_sustained, person_count_exceeds_policy, rapid_motion_in_restricted_zone. |
| L3 | VLM Deep Analysis | Qwen 2.5 VL, 5 s to 1 min. Prompt-based classify ("Is the person in danger? Why?"), KB retrieval via pgvector before the VLM call, NL output (label + rationale + confidence). |
| L4 | AI Agent | LangGraph + rules. Policy rules (guards, schedules, zones), incident FSM (new -> alert -> resolved/dismissed), confidence calibration (CV + VLM + KB fusion with documented weights), KB write-back (no retraining). |
| L5 | Persistence + KB | 7-day local NVMe. Postgres + MinIO (incidents, clips, audit, indexed by time/cam/type), pgvector KB (prompts, reasons, embeddings), 30 s pre-incident ring buffer per camera attached to alerts. |
| L6 | Service Plane | FastAPI + WebSocket + dispatcher. REST API (incidents, search, feedback), Next.js review dashboard, alert dispatcher (webhook stub; SMS/Teams/ACC documented as production paths), feedback loop (operator label -> KB update, no retraining). |

## Slices

Build in order. Each slice must leave the system end-to-end runnable.

- [x] **Slice 1 — Skeleton.** Docker infra (Postgres+pgvector, MinIO, Redis), Incident schema, persistence model + Alembic migration, stub pipeline (video -> YOLOv8n -> stub incident), FastAPI service plane (`/health`, `/process_video`, `/incidents`).
- [x] **Slice 2 — L2 perception.** Add real pose (RTMPose) and action (SlowFast) alongside the existing YOLOv8 detection. Replace the aspect-ratio fall rule with pose geometry. Notes: RTMPose runs via rtmlib/ONNX (mmcv has no wheel for torch 2.12+cu130, no nvcc to source-build); SlowFast preprocessing is hand-rolled (pytorchvideo.transforms broken on torchvision 0.27); pose-fall eval results in `evals/results/pose_baseline.json`.
- [x] **Slice 3 — Tracker.** ByteTrack persistent identity in the L2 pipeline. Per-track fall persistence and pose history (maxlen=32). ROI crop deferred to Slice 9 (not in the ingest path yet).
- [x] **Slice 4 — Event Gate.** Rules over L2 outputs that funnel ~1% of frames forward; everything else filtered. Reference rules plus two extensions:
  - `prolonged_inactivity_in_private_zone` — pose model shows minimal joint movement over N minutes in a private zone (bathroom, bedroom); possible medical event.
  - `unattended_minor_in_high_risk_zone` — a single small bounding box in a zone tagged kitchen/pool/garage with no adult-sized box nearby. Bbox area used as age proxy (Slice 4 limitation; real deployment needs a face age classifier).
  - Room policies in `config/rooms.yaml`. `process_video` returns `ProcessVideoResult` (TypedDict) with `incidents_created`, `frames_processed`, `frames_escalated`, `escalation_ratio`. L3 stub at `services/vlm/stub.py`.
  - Mini-persistence (N=3 frames) on `fall_pose_detected` in the gate cuts no-fall false alarms from 16% to 9.6% on Le2i Coffee_room. Sanity-checked: in-fall recall 87.9%, pre-fall false alarms 0%.
- [x] **Slice 5 — Real VLM.** Qwen 2.5 VL 7B via HF Inference Providers. Stub fallback via VLM_MODE env var (real/auto/stub). `{kb_context}` placeholder ready for Slice 6 KB retrieval. Evidence clips written to MinIO deferred (MinIO wired but clip bundling not yet built).
- [x] **Slice 6 — KB and memory.** pgvector knowledge base with cosine similarity search. `sentence-transformers/all-mpnet-base-v2` (768-dim) embedder singleton. Migration `0002_create_kb_entries.py` with HNSW index. `analyze_escalated` now returns `(VLMResult, reason, prompt_used)` — the full prompt sent to the model, including any KB context. KB context (top-3 similar incidents, threshold=0.7) injected into VLM prompt before each real call. KB entry written on each incident created from a non-stub VLM result (`operator_decision="pending"` until Slice 7). `KBEntryRow` in a separate `KBBase` so existing SQLite incident tests are unaffected. KB tests use testcontainers (`pgvector/pgvector:pg16`) and are skipped gracefully when Docker is unavailable.
- [x] **Slice 7 — Agent layer.** LangGraph 6-node StateGraph replaces direct pipeline incident writes. Nodes: parse_vlm_output (label -> severity mapping, stub defaults), policy_check (loads `config/policies/<facility_id>.yaml`; three rule types), confidence_fusion (weighted yolo/pose/action/vlm/kb with proportional weight redistribution for missing sources; weights loaded from policy YAML), decide (ALERT or DISMISS; stub caution requires fired rules + threshold+0.1 when VLM is stub), kb_writeback (replaces Slice 6 pipeline write; operator_decision="pending" for alert, "agent_dismissed" for dismiss), persist (writes incident + incident_audit row). Migration `0003_create_incident_audit.py`. 29 new tests; 145 passing total.
- [x] **Slice 7.5 — Incident Replay.** `POST /incidents/{id}/replay` re-runs the original clip through the current pipeline state (current KB, current rules) and returns the new outcome alongside the original. Tests whether the growing KB changes decisions over time. `CLIP_BUFFER_LEN=90` pre-incident ring buffer (~3 s at 30 fps; 30 s is the production target, documented in `services/pipeline/replay.py`). Evidence clips saved to `clips/{incident_id}.mp4` by the agent persist node. Replay runs `dry_run=True` — no DB or filesystem writes. Structured `changed` dict: `state_changed`, `confidence_delta`, `rationale_changed`, `any_change`. 7 new tests (6 replay + 1 persist dry_run); 152 passing total.
- [x] **Slice 8 — Dashboard.** Next.js operator UI over the REST API + WebSocket alerts, with the operator feedback loop writing back to the KB. Split into three sub-slices:
  - [x] **Slice 8a — Backend extensions.** `GET /incidents` with 6 filters (camera_id, room_id, state, severity, from/to datetime, limit/offset). `GET /incidents/{id}` full detail (vlm_prompt, fired_rules, confidence_breakdown, kb_matches, operator_decision). `POST /incidents/{id}/feedback` (updates operator_decision, writes KB entry; stub-origin tagged with vlm_source="stub"). `GET /metrics` (in-memory runtime counters + DB-computed historical stats). `GET /architecture` (six-layer status document). WebSocket `GET /ws/alerts` (in-memory pub/sub; Redis is the production path). Migration `0004_extend_incident_detail.py` adds 5 nullable columns to incidents table. Agent graph updated to persist confidence_breakdown and kb_matches. 17 new tests; 169 passing total.
  - [x] **Slice 8b — Dashboard core.** Next.js 14 App Router scaffold. Dark mode. Three pages: `/` live feed (WebSocket with exponential backoff reconnect + 30 s ping), `/incidents/[id]` detail (clip player, rationale, fired rules, confidence breakdown table, KB matches collapsible, confirm/dismiss feedback), `/history` (filter form + paginated table). Two components: `AlertCard` (severity color-coded), `LayerStatusBar` (6-dot status strip). `useAlertFeed` hook with stable callback ref. `StaticFiles` mount at `/clips` in FastAPI. Dashboard `Dockerfile` (Node 22 alpine) and docker-compose service (port 3000). 5 tests passing (2 AlertCard snapshots, 1 LayerStatusBar snapshot + dot count, 1 WebSocket integration).
  - [x] **Slice 8c — Inspector, Metrics, Architecture pages.**
  - [x] **Slice 8d — Live pipeline progress animation.** `progress_callback: Optional[Callable]` added to `process_video()`. Seven emit points (L1 complete, L2 processing/complete, gate complete, L3 processing/complete, L4 processing/complete, L5 complete), each fired at most once per call using flags. `app.py` wires a sync callback via `asyncio.run_coroutine_threadsafe` to broadcast `{type: "pipeline_progress", layer, status}` events to WebSocket clients. `PipelineProgressContext` (React context + provider) holds `{layer, status}` with high-water-mark logic preventing backward animation. `useAlertFeed` forwards `pipeline_progress` messages to an optional `onProgress` callback. `AlertFeed` wires this to the context dispatcher. `LayerStatusBar` reads context: dots before active layer show emerald, active layer pulses blue, dots after stay zinc. Label shows "Pipeline: idle / processing L3 (VLM) / complete". 2s idle timeout resets all dots. 9 backend tests (5 new); 41 frontend tests (10 new); build clean. `GET /incidents/{id}/inspect` page with per-layer trace (L1 camera/room, L2 confidence breakdown, Event Gate fired rules, L3 VLM prompt, L4 FSM audit trail, L5 KB matches). `AuditEntry` schema + `audit_entries` on `IncidentDetail`. `GET /metrics` extended with `kb_entry_count` + `incidents_by_severity_24h`. Metrics page polls every 5 s. Architecture page reads `/architecture` endpoint (server-rendered; expandable layer cards with real/approximated/not_built badges). `LayerStatusBar` converted to Client Component — clicking a dot opens `ProvenanceDisclosure` modal. `ConfidenceBreakdown` component for inspector view. 12 backend tests; 31 frontend tests passing. Build: 8 routes, clean.
- [x] **Slice 9 — Polish + docs + demo.** `README.md` full rewrite (quickstart, real/simulated table, eval results, beyond-spec additions, tech stack). `docs/ARCHITECTURE.md` (six-layer ASCII diagram, layer status, data flow). `docs/DESIGN_DECISIONS.md` (11 decisions). `docs/KNOWN_LIMITATIONS.md` (10 limitations with production paths). `scripts/demo.sh` + `scripts/run_example.sh`. `.env.example` with all four vars.

Legend: `[ ]` not started, `[~]` in progress, `[x]` complete.
